{
  "name": "election-components",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "clean": "rimraf build",
    "build": "npm run clean && rollup -f cjs -i src/index.js -o build/index.js",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "add-commas": "0.0.4",
    "lodash": "^3.10.1",
    "united-states": "^1.0.0"
  },
  "devDependencies": {
    "rimraf": "^2.5.0",
    "rollup": "^0.24.0"
  }
}
